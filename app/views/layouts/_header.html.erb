<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-1"></div>
      <div class="col-sm-1">
        <div>
          <%= link_to "BOOKSTORE",root_path, id: "logo" %>
        </div>
      </div>
      <div class="col-sm-2"></div>
      <div class="col-sm-7">
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Book Requests", book_requests_path %></li>
          <% if current_user %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= t('.new_post') %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t('.post_for_sell'), new_book_url %></li>
                <li><%= link_to t('.post_for_buy'), new_book_request_url %></li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t('.profile'), user_path(id: session[:user_id], tag: "profile") %></li>
                <li class="divider"></li>
                <li><%= link_to t('.logout'), sessions_path, method: :delete, data: { confirm: t('.logout_confirm')} %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to t('.login'), new_session_path %></li>
            <li><%= link_to t('.sign_up'), new_user_path %></li>
          <% end %>
        </ul>
      </div>
      <div class="col-sm-1"></div>
    </div>
  </div>
</nav>
